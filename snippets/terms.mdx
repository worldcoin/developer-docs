

export const AuthenticatorSDK = () => {
    return (
        <Tooltip tip="The Authenticator SDK is called WalletKit">
            <a href="https://github.com/worldcoin/walletkit" target="_blank" rel="noopener noreferrer">
                Authenticator SDK
            </a>
        </Tooltip>
    )
}

export const UniquenessProof = () => {
    return (
        <Tooltip tip="A Uniqueness Proof is a proof that the user has only performed the action once">
            Uniqueness Proof
        </Tooltip>
    )
}

export const RpRegistry = () => {
    return (
        <Tooltip tip="The Relying Party Registry is a smart contract where RPs can register before using the Protocol.">
            Relying Party Registry
        </Tooltip>
    )
}

export const Nullifier = () => {
    return (
        <Tooltip tip="The nullifier is a one-time use token which is used to ensure a single action is performed only once.">
            Nullifier
        </Tooltip>
    )
}